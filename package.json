{
  "name": "right-trade",
  "private": true,
  "version": "0.1.0",
  "description": "Right Trade — mobile-first trading automation platform (MVP monorepo)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "dev:api": "npm run build:shared && npm run dev -w apps/api",
    "build:api": "npm run build:shared && npm run build -w apps/api",
    "test:api": "npm run build:shared && npm run test -w apps/api",
    "seed:api": "npm run build:shared && npm run seed -w apps/api",
    "start:mobile": "npm run build:shared && npm run start -w apps/mobile",
    "typecheck": "npm run typecheck -w packages/shared && npm run build:shared && npm run typecheck -w apps/api && npm run typecheck -w apps/mobile"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "5.5"
  }
}
